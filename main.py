from rectangle import rangular
from triangle import  triangle
rec = rangular()
tri = triangle()
rec.set_values(50 , 40)
tri.set_values(30, 60)
print(rec.area_of_rectangular())
print(tri.area_of_triangle())